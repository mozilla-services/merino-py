name: publish-gar-image-fleece

on:
  workflow_call:
    inputs:
      image_tag:
        required: true
        type: string
      project_id:
        required: true
        type: string
      workload_identity_pool_project_number:
        required: true
        type: string
    secrets:
      DOCKER_IMAGE_PATH:
        required: true

jobs:
  publish:
    environment: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Download image artifact
        uses: actions/download-artifact@v7
        with:
          name: merino-fleece
          path: .
      - name: Load Docker image
        run: gunzip -c fleece-image.tar.gz | docker load
      - name: Tag image with GAR path
        run: docker tag app-fleece:build ${{ secrets.DOCKER_IMAGE_PATH }}/merino-fleece:${{ inputs.image_tag }}
      - name: Push image to GAR
        uses: mozilla/deploy-actions/docker-push@v3.11.1
        with:
          local_image: app-fleece:build
          image_repo_path: ${{ secrets.DOCKER_IMAGE_PATH }}/merino-fleece
          image_tag: ${{ inputs.image_tag }}
          workload_identity_pool_project_number: ${{ inputs.workload_identity_pool_project_number }}
          project_id: ${{ inputs.project_id }}
